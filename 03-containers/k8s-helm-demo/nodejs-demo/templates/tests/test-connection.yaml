apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejs-demo.fullname" . }}-test-connection"
  labels:
    {{- include "nodejs-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejs-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
